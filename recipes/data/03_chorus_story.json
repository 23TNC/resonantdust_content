{
  "_comment": "Chorus-faction story recipes. Each beat moves the player toward (or away from) the chorus faction by mutating `Player.flags.faction` via the recipe DSL's `<owner-chain>.aspect.faction.set` verb (see action_completion::Effect::SetPlayerFaction). The Faction* aliases in recipes/aliases.json resolve to the 2-bit slot values (FactionChorus=1, etc.).",

  "chorus_attune": {
    "input": [
      "use.Hex.0.def_id: alter",
      "claim.Top.0.aspect.corpse.min: 1"
    ],
    "output": [
      "sys.duration.set: 10",
      "Top.0.style.set: ltr",
      "Hex.0.owner.aspect.faction.set: FactionChorus",
      "Top.0.destroy"
    ]
  }
}
